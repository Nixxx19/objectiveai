# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Optional, Union
from pydantic import BaseModel, Field


class VectorCompletionsResponseVote(BaseModel):
    """A single LLM's vote in a vector completion.

Each LLM in the ensemble produces a vote indicating which response(s) it
selected. Votes are weighted according to the profile and combined to
produce the final scores.

# Vote Format

The `vote` field is a vector of decimals corresponding to the responses
in the request. Typically one element is 1.0 and the rest are 0.0 (discrete
selection), but when `top_logprobs` is used, votes may be probability
distributions."""
    agent: str = Field(..., description='The agent that produced this vote (content-addressed ID).')
    ensemble_index: int = Field(..., description='Index of the agent configuration within the ensemble.', ge=0, json_schema_extra={'format': 'uint64'})
    flat_ensemble_index: int = Field(..., description='Flattened index accounting for agent counts in the ensemble.', ge=0, json_schema_extra={'format': 'uint64'})
    prompt_id: str = Field(..., description='Content hash of the request messages (for caching/deduplication).')
    responses_ids: list[str] = Field(..., description='Content hashes of each response option in the request.')
    vote: list[float] = Field(..., description='The vote distribution. Each index corresponds to a response from the\nrequest. Typically one element is 1.0 (selected) and the rest are 0.0.')
    weight: float = Field(..., description='The weight applied to this vote when computing final scores.', json_schema_extra={'format': 'double'})
    retry: Optional[Union[bool, None]] = Field(None, description="If true, this vote was reused from a previous request via the `retry`\nparameter. All fields reflect the original request's values.")
    from_cache: Optional[Union[bool, None]] = Field(None, description='If true, this vote was retrieved from cache rather than generated fresh.')

