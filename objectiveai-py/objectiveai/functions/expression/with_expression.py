# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Optional, TYPE_CHECKING, Union
from pydantic import RootModel
from objectiveai.agent.completions.message.file import AgentCompletionsMessageFile
from objectiveai.agent.completions.message.image_url import AgentCompletionsMessageImageUrl
from objectiveai.agent.completions.message.input_audio import AgentCompletionsMessageInputAudio
from objectiveai.agent.completions.message.video_url import AgentCompletionsMessageVideoUrl
from objectiveai.functions.expression.expression import FunctionsExpressionExpression

if TYPE_CHECKING:
    from objectiveai.agent.completions.message.assistant_tool_call_expression import AgentCompletionsMessageAssistantToolCallExpression
    from objectiveai.agent.completions.message.assistant_tool_call_function_expression import AgentCompletionsMessageAssistantToolCallFunctionExpression
    from objectiveai.agent.completions.message.message_expression import AgentCompletionsMessageMessageExpression
    from objectiveai.agent.completions.message.rich_content_expression import AgentCompletionsMessageRichContentExpression
    from objectiveai.agent.completions.message.rich_content_part_expression import AgentCompletionsMessageRichContentPartExpression
    from objectiveai.agent.completions.message.simple_content_expression import AgentCompletionsMessageSimpleContentExpression
    from objectiveai.agent.completions.message.simple_content_part_expression import AgentCompletionsMessageSimpleContentPartExpression
    from objectiveai.functions.expression.input_expression import FunctionsExpressionInputExpression


class FunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageAssistantToolCallExpression]


class FunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallFunctionExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageAssistantToolCallFunctionExpression]


class FunctionsExpressionWithExpressionAgentCompletionsMessageFile(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageFile]


class FunctionsExpressionWithExpressionAgentCompletionsMessageImageUrl(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageImageUrl]


class FunctionsExpressionWithExpressionAgentCompletionsMessageInputAudio(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageInputAudio]


class FunctionsExpressionWithExpressionAgentCompletionsMessageMessageExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageMessageExpression]


class FunctionsExpressionWithExpressionAgentCompletionsMessageRichContentExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageRichContentExpression]


class FunctionsExpressionWithExpressionAgentCompletionsMessageRichContentPartExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageRichContentPartExpression]


class FunctionsExpressionWithExpressionAgentCompletionsMessageSimpleContentExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageSimpleContentExpression]


class FunctionsExpressionWithExpressionAgentCompletionsMessageSimpleContentPartExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageSimpleContentPartExpression]


class FunctionsExpressionWithExpressionAgentCompletionsMessageVideoUrl(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, AgentCompletionsMessageVideoUrl]


class FunctionsExpressionWithExpressionArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageMessageExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, list[FunctionsExpressionWithExpressionAgentCompletionsMessageMessageExpression]]


class FunctionsExpressionWithExpressionArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageRichContentExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, list[FunctionsExpressionWithExpressionAgentCompletionsMessageRichContentExpression]]


class FunctionsExpressionWithExpressionFunctionsExpressionInputExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, FunctionsExpressionInputExpression]


class FunctionsExpressionWithExpressionNullableAgentCompletionsMessageRichContentExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, Optional[AgentCompletionsMessageRichContentExpression]]


class FunctionsExpressionWithExpressionNullableArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, Union[list[FunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallExpression], None]]


class FunctionsExpressionWithExpressionNullableString(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, Union[str, None]]


class FunctionsExpressionWithExpressionString(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    root: Union[FunctionsExpressionExpression, str]

