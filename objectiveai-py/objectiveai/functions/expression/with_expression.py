# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Optional, Union
from pydantic import ConfigDict, RootModel
from objectiveai.agent.completions.message.file import File
from objectiveai.agent.completions.message.image_url import ImageUrl
from objectiveai.agent.completions.message.input_audio import InputAudio
from objectiveai.agent.completions.message.video_url import VideoUrl
from objectiveai.functions.expression.expression import Expression


class WithExpression_AgentCompletionsMessageAssistantToolCallExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageAssistantToolCallExpressionVariant2(RootModel):
    """A literal value."""
    root: AssistantToolCallExpression


class WithExpression_AgentCompletionsMessageAssistantToolCallExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.AssistantToolCallExpression')

    root: Union[WithExpression_AgentCompletionsMessageAssistantToolCallExpressionVariant1, WithExpression_AgentCompletionsMessageAssistantToolCallExpressionVariant2]


class WithExpression_AgentCompletionsMessageAssistantToolCallFunctionExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageAssistantToolCallFunctionExpressionVariant2(RootModel):
    """A literal value."""
    root: AssistantToolCallFunctionExpression


class WithExpression_AgentCompletionsMessageAssistantToolCallFunctionExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.AssistantToolCallFunctionExpression')

    root: Union[WithExpression_AgentCompletionsMessageAssistantToolCallFunctionExpressionVariant1, WithExpression_AgentCompletionsMessageAssistantToolCallFunctionExpressionVariant2]


class WithExpression_AgentCompletionsMessageFileVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageFileVariant2(RootModel):
    """A literal value."""
    root: File


class WithExpression_AgentCompletionsMessageFile(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.File')

    root: Union[WithExpression_AgentCompletionsMessageFileVariant1, WithExpression_AgentCompletionsMessageFileVariant2]


class WithExpression_AgentCompletionsMessageImageUrlVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageImageUrlVariant2(RootModel):
    """A literal value."""
    root: ImageUrl


class WithExpression_AgentCompletionsMessageImageUrl(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.ImageUrl')

    root: Union[WithExpression_AgentCompletionsMessageImageUrlVariant1, WithExpression_AgentCompletionsMessageImageUrlVariant2]


class WithExpression_AgentCompletionsMessageInputAudioVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageInputAudioVariant2(RootModel):
    """A literal value."""
    root: InputAudio


class WithExpression_AgentCompletionsMessageInputAudio(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.InputAudio')

    root: Union[WithExpression_AgentCompletionsMessageInputAudioVariant1, WithExpression_AgentCompletionsMessageInputAudioVariant2]


class WithExpression_AgentCompletionsMessageMessageExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageMessageExpressionVariant2(RootModel):
    """A literal value."""
    root: MessageExpression


class WithExpression_AgentCompletionsMessageMessageExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.MessageExpression')

    root: Union[WithExpression_AgentCompletionsMessageMessageExpressionVariant1, WithExpression_AgentCompletionsMessageMessageExpressionVariant2]


class WithExpression_AgentCompletionsMessageRichContentExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageRichContentExpressionVariant2(RootModel):
    """A literal value."""
    root: RichContentExpression


class WithExpression_AgentCompletionsMessageRichContentExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.RichContentExpression')

    root: Union[WithExpression_AgentCompletionsMessageRichContentExpressionVariant1, WithExpression_AgentCompletionsMessageRichContentExpressionVariant2]


class WithExpression_AgentCompletionsMessageRichContentPartExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageRichContentPartExpressionVariant2(RootModel):
    """A literal value."""
    root: RichContentPartExpression


class WithExpression_AgentCompletionsMessageRichContentPartExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.RichContentPartExpression')

    root: Union[WithExpression_AgentCompletionsMessageRichContentPartExpressionVariant1, WithExpression_AgentCompletionsMessageRichContentPartExpressionVariant2]


class WithExpression_AgentCompletionsMessageSimpleContentExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageSimpleContentExpressionVariant2(RootModel):
    """A literal value."""
    root: SimpleContentExpression


class WithExpression_AgentCompletionsMessageSimpleContentExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.SimpleContentExpression')

    root: Union[WithExpression_AgentCompletionsMessageSimpleContentExpressionVariant1, WithExpression_AgentCompletionsMessageSimpleContentExpressionVariant2]


class WithExpression_AgentCompletionsMessageSimpleContentPartExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageSimpleContentPartExpressionVariant2(RootModel):
    """A literal value."""
    root: SimpleContentPartExpression


class WithExpression_AgentCompletionsMessageSimpleContentPartExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.SimpleContentPartExpression')

    root: Union[WithExpression_AgentCompletionsMessageSimpleContentPartExpressionVariant1, WithExpression_AgentCompletionsMessageSimpleContentPartExpressionVariant2]


class WithExpression_AgentCompletionsMessageVideoUrlVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_AgentCompletionsMessageVideoUrlVariant2(RootModel):
    """A literal value."""
    root: VideoUrl


class WithExpression_AgentCompletionsMessageVideoUrl(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.agent.completions.message.VideoUrl')

    root: Union[WithExpression_AgentCompletionsMessageVideoUrlVariant1, WithExpression_AgentCompletionsMessageVideoUrlVariant2]


class WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageMessageExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageMessageExpressionVariant2(RootModel):
    """A literal value."""
    root: list[WithExpression_AgentCompletionsMessageMessageExpression]


class WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageMessageExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.Array_of_functions.expression.WithExpression.agent.completions.message.MessageExpression')

    root: Union[WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageMessageExpressionVariant1, WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageMessageExpressionVariant2]


class WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageRichContentExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageRichContentExpressionVariant2(RootModel):
    """A literal value."""
    root: list[WithExpression_AgentCompletionsMessageRichContentExpression]


class WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageRichContentExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.Array_of_functions.expression.WithExpression.agent.completions.message.RichContentExpression')

    root: Union[WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageRichContentExpressionVariant1, WithExpression_ArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageRichContentExpressionVariant2]


class WithExpression_FunctionsExpressionInputValueExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_FunctionsExpressionInputValueExpressionVariant2(RootModel):
    """A literal value."""
    root: InputValueExpression


class WithExpression_FunctionsExpressionInputValueExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.functions.expression.InputValueExpression')

    root: Union[WithExpression_FunctionsExpressionInputValueExpressionVariant1, WithExpression_FunctionsExpressionInputValueExpressionVariant2]


class WithExpression_NullableAgentCompletionsMessageRichContentExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_NullableAgentCompletionsMessageRichContentExpressionVariant2(RootModel):
    """A literal value."""
    root: Optional[RichContentExpression]


class WithExpression_NullableAgentCompletionsMessageRichContentExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.Nullable_agent.completions.message.RichContentExpression')

    root: Union[WithExpression_NullableAgentCompletionsMessageRichContentExpressionVariant1, WithExpression_NullableAgentCompletionsMessageRichContentExpressionVariant2]


class WithExpression_NullableArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallExpressionVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_NullableArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallExpressionVariant2(RootModel):
    """A literal value."""
    root: Optional[list[WithExpression_AgentCompletionsMessageAssistantToolCallExpression]]


class WithExpression_NullableArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallExpression(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.Nullable_Array_of_functions.expression.WithExpression.agent.completions.message.AssistantToolCallExpression')

    root: Union[WithExpression_NullableArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallExpressionVariant1, WithExpression_NullableArrayOfFunctionsExpressionWithExpressionAgentCompletionsMessageAssistantToolCallExpressionVariant2]


class WithExpression_NullableStringVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_NullableStringVariant2(RootModel):
    """A literal value."""
    root: Optional[str]


class WithExpression_NullableString(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.Nullable_string')

    root: Union[WithExpression_NullableStringVariant1, WithExpression_NullableStringVariant2]


class WithExpression_stringVariant1(RootModel):
    """An expression (JMESPath or Starlark) to evaluate."""
    root: Expression


class WithExpression_stringVariant2(RootModel):
    """A literal value."""
    root: str


class WithExpression_string(RootModel):
    """A value that can be either a literal or an expression.

This allows Function definitions to mix static values with dynamic
expressions. During compilation, expressions are evaluated while
literal values pass through unchanged.

# Example

Literal value:
```json
"hello world"
```

JMESPath expression:
```json
{"$jmespath": "input.greeting"}
```

Starlark expression:
```json
{"$starlark": "input['greeting']"}
```"""
    model_config = ConfigDict(title='functions.expression.WithExpression.string')

    root: Union[WithExpression_stringVariant1, WithExpression_stringVariant2]


# Deferred imports to break circular dependencies
from objectiveai.agent.completions.message.assistant_tool_call_expression import AssistantToolCallExpression  # noqa: E402
from objectiveai.agent.completions.message.assistant_tool_call_function_expression import AssistantToolCallFunctionExpression  # noqa: E402
from objectiveai.agent.completions.message.message_expression import MessageExpression  # noqa: E402
from objectiveai.agent.completions.message.rich_content_expression import RichContentExpression  # noqa: E402
from objectiveai.agent.completions.message.rich_content_part_expression import RichContentPartExpression  # noqa: E402
from objectiveai.agent.completions.message.simple_content_expression import SimpleContentExpression  # noqa: E402
from objectiveai.agent.completions.message.simple_content_part_expression import SimpleContentPartExpression  # noqa: E402
from objectiveai.functions.expression.input_value_expression import InputValueExpression  # noqa: E402
