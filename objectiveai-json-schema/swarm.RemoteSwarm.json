{
  "title": "swarm.RemoteSwarm",
  "description": "A validated remote Swarm with metadata and computed content-addressed ID.",
  "type": "object",
  "$ref": "swarm.InlineSwarm",
  "properties": {
    "description": {
      "type": "string"
    }
  }
}
