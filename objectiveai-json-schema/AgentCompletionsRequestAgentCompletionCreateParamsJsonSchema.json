{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AgentCompletionsRequestAgentCompletionCreateParams",
  "description": "Parameters for creating a agent completion.",
  "type": "object",
  "properties": {
    "messages": {
      "description": "The conversation messages.",
      "type": "array",
      "items": {
        "$ref": "AgentCompletionsMessageMessage"
      }
    },
    "provider": {
      "description": "Provider routing preferences.",
      "anyOf": [
        {
          "$ref": "AgentCompletionsRequestProvider"
        },
        {
          "type": "null"
        }
      ]
    },
    "agent": {
      "description": "The agent to use (inline Agent or stored ID).",
      "$ref": "AgentCompletionsRequestAgent"
    },
    "agents": {
      "description": "Alternative agents to try if the primary agent fails.",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "AgentCompletionsRequestAgent"
      }
    },
    "response_format": {
      "description": "Output format constraints (text, JSON, or JSON schema).",
      "anyOf": [
        {
          "$ref": "AgentCompletionsRequestResponseFormatParam"
        },
        {
          "type": "null"
        }
      ]
    },
    "seed": {
      "description": "Random seed for deterministic generation.",
      "type": [
        "integer",
        "null"
      ],
      "format": "int64"
    },
    "stream": {
      "description": "Whether to stream the response.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "mcp_server_authorization": {
      "description": "Map from MCP server URL to authorization header value.",
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "required": [
    "messages",
    "agent"
  ]
}
