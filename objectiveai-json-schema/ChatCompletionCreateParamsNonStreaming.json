{
  "type": "object",
  "properties": {
    "upstreams": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "$ref": "Upstream",
            "description": "The upstream provider that served the request."
          }
        },
        {
          "type": "null"
        }
      ],
      "description": "Available upstreams for this request."
    },
    "messages": {
      "type": "array",
      "items": {
        "$ref": "Message",
        "description": "A message exchanged in a chat conversation."
      },
      "description": "A list of messages exchanged in a chat conversation."
    },
    "provider": {
      "anyOf": [
        {
          "$ref": "CompletionProvider",
          "description": "Options for selecting the upstream provider of this completion."
        },
        {
          "type": "null"
        }
      ]
    },
    "model": {
      "anyOf": [
        {
          "type": "string",
          "description": "The unique identifier for the Ensemble LLM."
        },
        {
          "$ref": "EnsembleLlmBase",
          "description": "An LLM to be used within an Ensemble or standalone with Chat Completions."
        }
      ],
      "description": "The Ensemble LLM to use for this completion. May be a unique ID or an inline definition."
    },
    "models": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "string",
                "description": "The unique identifier for the Ensemble LLM."
              },
              {
                "$ref": "EnsembleLlmBase",
                "description": "An LLM to be used within an Ensemble or standalone with Chat Completions."
              }
            ],
            "description": "The Ensemble LLM to use for this completion. May be a unique ID or an inline definition."
          },
          "description": "Fallback Ensemble LLMs to use if the primary Ensemble LLM fails."
        },
        {
          "type": "null"
        }
      ]
    },
    "top_logprobs": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "description": "An integer between 0 and 20 specifying the number of most likely tokens to return at each token position, each with an associated log probability."
    },
    "response_format": {
      "anyOf": [
        {
          "$ref": "ResponseFormat",
          "description": "The desired format of the model's response."
        },
        {
          "type": "null"
        }
      ]
    },
    "seed": {
      "anyOf": [
        {
          "description": "If specified, upstream systems will make a best effort to sample deterministically, such that repeated requests with the same seed and parameters should return the same result."
        },
        {
          "type": "null"
        }
      ]
    },
    "tool_choice": {
      "anyOf": [
        {
          "anyOf": [
            {
              "const": "none"
            },
            {
              "const": "auto"
            },
            {
              "const": "required"
            },
            {
              "type": "object",
              "properties": {
                "type": {
                  "const": "function"
                },
                "function": {
                  "type": "object",
                  "properties": {
                    "name": {
                      "type": "string",
                      "description": "The name of the function the assistant will call."
                    }
                  },
                  "required": [
                    "name"
                  ]
                }
              },
              "required": [
                "type",
                "function"
              ],
              "description": "Specify a function tool for the assistant to call."
            }
          ],
          "description": "Specifies tool call behavior for the assistant."
        },
        {
          "type": "null"
        }
      ]
    },
    "tools": {
      "type": "array",
      "items": {
        "$ref": "Tool",
        "description": "A tool that the assistant can call."
      },
      "description": "A list of tools that the assistant can call."
    },
    "parallel_tool_calls": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "description": "Whether to allow the model to make multiple tool calls in parallel."
    },
    "prediction": {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "content"
            },
            "content": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "type": {
                        "const": "text"
                      },
                      "text": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "type",
                      "text"
                    ],
                    "description": "A part of the predicted content."
                  }
                }
              ]
            }
          },
          "required": [
            "type",
            "content"
          ],
          "description": "Configuration for a Predicted Output, which can greatly improve response times when large parts of the model response are known ahead of time. This is most common when you are regenerating a file with only minor changes to most of the content."
        },
        {
          "type": "null"
        }
      ]
    },
    "backoff_max_elapsed_time": {
      "anyOf": [
        {
          "type": "integer",
          "description": "The maximum total time in milliseconds to spend on retries when a transient error occurs."
        },
        {
          "type": "null"
        }
      ]
    },
    "first_chunk_timeout": {
      "anyOf": [
        {
          "type": "integer",
          "description": "The maximum time in milliseconds to wait for the first chunk of a streaming response."
        },
        {
          "type": "null"
        }
      ]
    },
    "other_chunk_timeout": {
      "anyOf": [
        {
          "type": "integer",
          "description": "The maximum time in milliseconds to wait between subsequent chunks of a streaming response."
        },
        {
          "type": "null"
        }
      ]
    },
    "stream": {
      "anyOf": [
        {
          "const": false,
          "description": "Whether to stream the response as a series of chunks."
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "messages",
    "model",
    "tools"
  ],
  "description": "Parameters for creating a unary chat completion."
}
