{
  "anyOf": [
    {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "tasks": {
              "type": "array",
              "items": {
                "$ref": "TaskProfile",
                "description": "The profile for a task."
              },
              "description": "The list of task profiles."
            },
            "profile": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "number"
                  },
                  "description": "Legacy representation: a simple list of weights. Must be of the same length as the Ensemble's `llms` field, ignoring count."
                },
                {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "weight": {
                        "type": "number",
                        "description": "The weight for this LLM in the ensemble. Must be between 0 and 1."
                      },
                      "invert": {
                        "anyOf": [
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "description": "If true, invert this LLM's vote distribution before combining. When omitted or false, the vote distribution is used as-is."
                      }
                    },
                    "required": [
                      "weight"
                    ],
                    "description": "An entry in a vector completion profile with an explicit weight and optional invert flag."
                  },
                  "description": "Profile entries with explicit `weight` and optional `invert` for each LLM in the ensemble."
                }
              ],
              "description": "The weights for each task used in weighted averaging of task outputs. Can be either a list of weights or a list of objects with `weight` and optional `invert`."
            }
          },
          "required": [
            "tasks",
            "profile"
          ],
          "description": "A tasks-based function profile defined inline."
        },
        {
          "type": "object",
          "properties": {
            "ensemble": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "The unique identifier for the Ensemble."
                },
                {
                  "$ref": "EnsembleBase",
                  "description": "An ensemble of LLMs."
                }
              ],
              "description": "The Ensemble to use for this completion. May be a unique ID or an inline definition."
            },
            "profile": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "number"
                  },
                  "description": "Legacy representation: a simple list of weights. Must be of the same length as the Ensemble's `llms` field, ignoring count."
                },
                {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "weight": {
                        "type": "number",
                        "description": "The weight for this LLM in the ensemble. Must be between 0 and 1."
                      },
                      "invert": {
                        "anyOf": [
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "description": "If true, invert this LLM's vote distribution before combining. When omitted or false, the vote distribution is used as-is."
                      }
                    },
                    "required": [
                      "weight"
                    ],
                    "description": "An entry in a vector completion profile with an explicit weight and optional invert flag."
                  },
                  "description": "Profile entries with explicit `weight` and optional `invert` for each LLM in the ensemble."
                }
              ],
              "description": "The weights for each LLM in the ensemble."
            }
          },
          "required": [
            "ensemble",
            "profile"
          ],
          "description": "An auto function profile defined inline. Applies a single ensemble and weights to all vector completion tasks."
        }
      ],
      "description": "A function profile defined inline."
    },
    {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "tasks": {
              "type": "array",
              "items": {
                "$ref": "TaskProfile",
                "description": "The profile for a task."
              },
              "description": "The list of task profiles."
            },
            "profile": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "number"
                  },
                  "description": "Legacy representation: a simple list of weights. Must be of the same length as the Ensemble's `llms` field, ignoring count."
                },
                {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "weight": {
                        "type": "number",
                        "description": "The weight for this LLM in the ensemble. Must be between 0 and 1."
                      },
                      "invert": {
                        "anyOf": [
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "description": "If true, invert this LLM's vote distribution before combining. When omitted or false, the vote distribution is used as-is."
                      }
                    },
                    "required": [
                      "weight"
                    ],
                    "description": "An entry in a vector completion profile with an explicit weight and optional invert flag."
                  },
                  "description": "Profile entries with explicit `weight` and optional `invert` for each LLM in the ensemble."
                }
              ],
              "description": "The weights for each task used in weighted averaging of task outputs. Can be either a list of weights or a list of objects with `weight` and optional `invert`."
            },
            "description": {
              "type": "string",
              "description": "The description of the profile."
            }
          },
          "required": [
            "tasks",
            "profile",
            "description"
          ],
          "description": "A remote tasks-based function profile. \"profile.json\""
        },
        {
          "type": "object",
          "properties": {
            "ensemble": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "The unique identifier for the Ensemble."
                },
                {
                  "$ref": "EnsembleBase",
                  "description": "An ensemble of LLMs."
                }
              ],
              "description": "The Ensemble to use for this completion. May be a unique ID or an inline definition."
            },
            "profile": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "number"
                  },
                  "description": "Legacy representation: a simple list of weights. Must be of the same length as the Ensemble's `llms` field, ignoring count."
                },
                {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "weight": {
                        "type": "number",
                        "description": "The weight for this LLM in the ensemble. Must be between 0 and 1."
                      },
                      "invert": {
                        "anyOf": [
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "description": "If true, invert this LLM's vote distribution before combining. When omitted or false, the vote distribution is used as-is."
                      }
                    },
                    "required": [
                      "weight"
                    ],
                    "description": "An entry in a vector completion profile with an explicit weight and optional invert flag."
                  },
                  "description": "Profile entries with explicit `weight` and optional `invert` for each LLM in the ensemble."
                }
              ],
              "description": "The weights for each LLM in the ensemble."
            },
            "description": {
              "type": "string",
              "description": "The description of the profile."
            }
          },
          "required": [
            "ensemble",
            "profile",
            "description"
          ],
          "description": "A remote auto function profile. \"profile.json\""
        }
      ],
      "description": "A remote function profile. \"profile.json\""
    }
  ],
  "description": "A function profile."
}
