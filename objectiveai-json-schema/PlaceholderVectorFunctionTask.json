{
  "type": "object",
  "properties": {
    "type": {
      "const": "placeholder.vector.function"
    },
    "input_schema": {
      "$ref": "InputSchema",
      "description": "An input schema defining the structure of function inputs."
    },
    "output_length": {
      "$ref": "Expression",
      "description": "An expression which evaluates to the length of the output vector. Receives: `input`."
    },
    "input_split": {
      "$ref": "Expression",
      "description": "Splits the function input into an array of sub-inputs, one per output element. Receives: `input`."
    },
    "input_merge": {
      "$ref": "Expression",
      "description": "Recombines a variable-size, arbitrarily-ordered subset of sub-inputs back into one input. Receives: `input` (an array of sub-inputs)."
    },
    "input": {
      "$ref": "InputValue",
      "description": "A value provided as input to a function."
    },
    "output": {
      "$ref": "Expression",
      "description": "Expression to transform the equalized vector output. Receives: `input`, `output` (the raw FunctionOutput)."
    }
  },
  "required": [
    "type",
    "input_schema",
    "output_length",
    "input_split",
    "input_merge",
    "input",
    "output"
  ],
  "description": "A placeholder vector function task. Always outputs an equalized vector."
}
