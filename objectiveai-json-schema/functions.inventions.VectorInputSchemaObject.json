{
  "title": "functions.inventions.VectorInputSchemaObject",
  "description": "Schema for WriteInputSchema on vector functions.\nTakes a `schema` property: a JSON string conforming to `functions.alpha_vector.expression.VectorFunctionInputSchema`.",
  "type": "object",
  "properties": {
    "schema": {
      "type": "string"
    }
  }
}
