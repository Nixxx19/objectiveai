{
  "title": "string.functions.expression.OneOrMany",
  "description": "Result of an expression that may produce one or many values.",
  "anyOf": [
    {
      "description": "A single value.",
      "type": "string"
    },
    {
      "description": "Multiple values (from array expressions).",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  ]
}
