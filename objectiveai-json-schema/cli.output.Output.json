{
  "title": "cli.output.Output",
  "description": "A single line of CLI output.",
  "anyOf": [
    {
      "title": "Error",
      "type": "object",
      "$ref": "cli.output.Error",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "error"
          ]
        }
      }
    },
    {
      "title": "Notification",
      "description": "Wraps [`NotificationValue`] in [`Notification`] so its fields\nend up under a nested `value` key.",
      "type": "object",
      "$ref": "cli.output.notification.Notification",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "notification"
          ]
        }
      }
    }
  ]
}
