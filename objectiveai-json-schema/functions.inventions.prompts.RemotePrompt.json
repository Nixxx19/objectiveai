{
  "title": "functions.inventions.prompts.RemotePrompt",
  "description": "A remote prompt with description and inline prompt fields.",
  "type": "object",
  "$ref": "functions.inventions.prompts.InlinePrompt",
  "properties": {
    "description": {
      "description": "Human-readable description of the prompt.",
      "type": "string"
    }
  }
}
