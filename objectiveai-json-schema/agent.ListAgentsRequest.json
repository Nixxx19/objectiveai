{
  "title": "agent.ListAgentsRequest",
  "description": "Query parameters for the list agents endpoint.",
  "type": "object",
  "properties": {
    "source": {
      "description": "Optional source filter for listing agents.",
      "anyOf": [
        {
          "$ref": "agent.ListAgentsSource"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
