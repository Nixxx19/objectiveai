{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AgentCompletionsResponseUsage",
  "description": "Aggregated token and cost usage for an agent completion.\n\nThis is the \"primary\" usage type that aggregates across all upstream\nassistant responses within a single agent completion.",
  "type": "object",
  "properties": {
    "completion_tokens": {
      "description": "Total tokens generated across all assistant responses.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "prompt_tokens": {
      "description": "Total prompt tokens across all assistant responses.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "total_tokens": {
      "description": "Sum of completion and prompt tokens.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "completion_tokens_details": {
      "description": "Breakdown of completion tokens (reasoning, audio, etc.) if available.",
      "anyOf": [
        {
          "$ref": "AgentCompletionsResponseCompletionTokensDetails"
        },
        {
          "type": "null"
        }
      ]
    },
    "prompt_tokens_details": {
      "description": "Breakdown of prompt tokens (cached, audio, etc.) if available.",
      "anyOf": [
        {
          "$ref": "AgentCompletionsResponsePromptTokensDetails"
        },
        {
          "type": "null"
        }
      ]
    },
    "cost": {
      "description": "Cost charged by ObjectiveAI for this request.",
      "type": [
        "string",
        "number"
      ],
      "pattern": "^-?\\d+(\\.\\d+)?([eE]\\d+)?$"
    },
    "cost_details": {
      "description": "Breakdown of upstream and upstream_upstream costs if available.",
      "anyOf": [
        {
          "$ref": "AgentCompletionsResponseCostDetails"
        },
        {
          "type": "null"
        }
      ]
    },
    "total_cost": {
      "description": "Total cost including upstream provider charges. Only differs from `cost`\nwhen using BYOK (Bring Your Own Key).",
      "type": [
        "string",
        "number"
      ],
      "pattern": "^-?\\d+(\\.\\d+)?([eE]\\d+)?$"
    }
  },
  "required": [
    "completion_tokens",
    "prompt_tokens",
    "total_tokens",
    "cost",
    "total_cost"
  ]
}
