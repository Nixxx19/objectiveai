{
  "type": "object",
  "properties": {
    "type": {
      "const": "alpha.scalar.leaf.function"
    },
    "description": {
      "type": "string",
      "description": "The description of the alpha scalar leaf function."
    },
    "input_schema": {
      "type": "object",
      "properties": {
        "type": {
          "const": "object"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "The description of the object input."
        },
        "properties": {
          "type": "object",
          "additionalProperties": {
            "$ref": "InputSchema"
          },
          "description": "The properties of the object input."
        },
        "required": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "null"
            }
          ],
          "description": "The required properties of the object input."
        }
      },
      "required": [
        "type",
        "properties"
      ],
      "description": "The input schema for the alpha scalar function."
    },
    "tasks": {
      "type": "array",
      "items": {
        "anyOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "const": "vector.completion"
              },
              "skip": {
                "$ref": "Expression",
                "description": "An expression which evaluates to a boolean indicating whether to skip this task. Receives: `input`."
              },
              "messages": {
                "$ref": "Expression",
                "description": "An expression which evaluates to the chat messages. Receives: `input`."
              },
              "responses": {
                "type": "array",
                "items": {
                  "anyOf": [
                    {
                      "type": "string",
                      "description": "Plain text content."
                    },
                    {
                      "type": "array",
                      "items": {
                        "$ref": "RichContentPart",
                        "description": "A rich content part."
                      },
                      "description": "An array of rich content parts."
                    }
                  ],
                  "description": "Rich content."
                },
                "description": "The response options for the vector completion."
              }
            },
            "required": [
              "type",
              "messages",
              "responses"
            ],
            "description": "A vector completion task expression for an alpha scalar leaf function."
          }
        ],
        "description": "A leaf task expression for an alpha scalar function."
      },
      "description": "The list of leaf tasks to be executed as part of the alpha scalar function."
    }
  },
  "required": [
    "type",
    "description",
    "input_schema",
    "tasks"
  ],
  "description": "An alpha scalar leaf remote function. \"function.json\""
}
