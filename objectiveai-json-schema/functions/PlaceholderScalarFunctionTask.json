{
  "type": "object",
  "properties": {
    "type": {
      "const": "placeholder.scalar.function"
    },
    "input_schema": {
      "$ref": "InputSchema",
      "description": "An input schema defining the structure of function inputs."
    },
    "input": {
      "$ref": "InputValue",
      "description": "A value provided as input to a function."
    },
    "output": {
      "$ref": "Expression",
      "description": "Expression to transform the fixed 0.5 output. Receives: `input`, `output` (the raw FunctionOutput)."
    }
  },
  "required": [
    "type",
    "input_schema",
    "input",
    "output"
  ],
  "description": "A placeholder scalar function task. Always outputs 0.5."
}
