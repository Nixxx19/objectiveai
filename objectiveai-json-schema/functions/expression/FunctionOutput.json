{
  "anyOf": [
    {
      "anyOf": [
        {
          "type": "number",
          "description": "A valid output of a scalar function execution / function execution task. A number between 0 and 1."
        },
        {
          "type": "array",
          "items": {
            "type": "number"
          },
          "description": "A valid output of a vector function execution / function execution task. A vector of numbers, summing to 1."
        }
      ],
      "description": "The valid output of a function execution / function execution task."
    },
    {
      "$ref": "JsonValue",
      "description": "The erroneous output of a function execution, containing the invalid output which failed validation."
    }
  ],
  "description": "The output of a function execution / function execution task."
}
