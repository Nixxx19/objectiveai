{
  "type": "array",
  "items": {
    "anyOf": [
      {
        "type": "array",
        "items": {
          "type": "number"
        },
        "description": "A valid output of a vector function execution / function execution task. A vector of numbers, summing to 1."
      },
      {
        "$ref": "JsonValue",
        "description": "The erroneous output of a function execution, containing the invalid output which failed validation."
      }
    ],
    "description": "The output of a vector function execution / function execution task."
  },
  "description": "The output of a mapped vector function execution task. An array of vector function outputs."
}
