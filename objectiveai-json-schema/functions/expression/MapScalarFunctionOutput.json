{
  "type": "array",
  "items": {
    "anyOf": [
      {
        "type": "number",
        "description": "A valid output of a scalar function execution / function execution task. A number between 0 and 1."
      },
      {
        "$ref": "JsonValue",
        "description": "The erroneous output of a function execution, containing the invalid output which failed validation."
      }
    ],
    "description": "The output of a scalar function execution / function execution task."
  },
  "description": "The output of a mapped scalar function execution task. An array of scalar function outputs."
}
