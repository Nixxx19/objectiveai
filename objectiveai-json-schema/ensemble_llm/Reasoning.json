{
  "type": "object",
  "properties": {
    "enabled": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "description": "Enables or disables reasoning for supported models."
    },
    "max_tokens": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "description": "The maximum number of tokens to use for reasoning in a response."
    },
    "effort": {
      "anyOf": [
        {
          "enum": [
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh"
          ],
          "description": "Constrains effort on reasoning for supported reasoning models. Reducing reasoning effort can result in faster responses and fewer tokens used on reasoning in a response."
        },
        {
          "type": "null"
        }
      ]
    },
    "summary_verbosity": {
      "anyOf": [
        {
          "enum": [
            "auto",
            "concise",
            "detailed"
          ],
          "description": "Controls the verbosity of the reasoning summary for supported reasoning models."
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "description": "Options for controlling reasoning behavior of the model."
}
