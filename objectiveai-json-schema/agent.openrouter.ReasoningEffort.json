{
  "title": "agent.openrouter.ReasoningEffort",
  "description": "The level of effort the model should put into reasoning.\n\nOnly supported by some models.",
  "anyOf": [
    {
      "title": "None",
      "description": "No reasoning.",
      "type": "string",
      "enum": [
        "none"
      ]
    },
    {
      "title": "Minimal",
      "description": "Minimal reasoning effort.",
      "type": "string",
      "enum": [
        "minimal"
      ]
    },
    {
      "title": "Low",
      "description": "Low reasoning effort.",
      "type": "string",
      "enum": [
        "low"
      ]
    },
    {
      "title": "Medium",
      "description": "Medium reasoning effort.",
      "type": "string",
      "enum": [
        "medium"
      ]
    },
    {
      "title": "High",
      "description": "High reasoning effort.",
      "type": "string",
      "enum": [
        "high"
      ]
    },
    {
      "title": "Xhigh",
      "description": "Maximum reasoning effort.",
      "type": "string",
      "enum": [
        "xhigh"
      ]
    }
  ]
}
