{
  "anyOf": [
    {
      "type": "object",
      "properties": {
        "type": {
          "const": "vector.completion"
        },
        "skip": {
          "$ref": "Expression",
          "description": "An expression which evaluates to a boolean indicating whether to skip this task. Receives: `input`."
        },
        "messages": {
          "$ref": "Expression",
          "description": "An expression which evaluates to the chat messages. Receives: `input`."
        },
        "responses": {
          "$ref": "Expression",
          "description": "An expression which evaluates to the response options for the vector completion. Receives: `input`."
        }
      },
      "required": [
        "type",
        "messages",
        "responses"
      ],
      "description": "A vector completion task expression for an alpha vector leaf function."
    }
  ],
  "description": "A leaf task expression for an alpha vector function."
}
