{
  "title": "swarm.RemoteSwarmBase",
  "description": "A remote swarm base definition with metadata (without computed ID).\n\nLike [`InlineSwarmBase`] but includes a description for remote storage.",
  "type": "object",
  "$ref": "swarm.InlineSwarmBase",
  "properties": {
    "description": {
      "description": "Human-readable description of what this swarm does.",
      "type": "string"
    }
  }
}
