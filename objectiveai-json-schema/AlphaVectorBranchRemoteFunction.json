{
  "type": "object",
  "properties": {
    "type": {
      "const": "alpha.vector.branch.function"
    },
    "description": {
      "type": "string",
      "description": "The description of the alpha vector branch function."
    },
    "input_schema": {
      "type": "object",
      "properties": {
        "context": {
          "type": "object",
          "properties": {
            "type": {
              "const": "object"
            },
            "description": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The description of the object input."
            },
            "properties": {
              "type": "object",
              "additionalProperties": {
                "$ref": "InputSchema"
              },
              "description": "The properties of the object input."
            },
            "required": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "null"
                }
              ],
              "description": "The required properties of the object input."
            }
          },
          "required": [
            "type",
            "properties"
          ],
          "description": "The optional context input schema shared across all items."
        },
        "items": {
          "$ref": "InputSchema",
          "description": "The input schema for each item."
        }
      },
      "required": [
        "items"
      ],
      "description": "The input schema for the alpha vector function."
    },
    "tasks": {
      "type": "array",
      "items": {
        "anyOf": [
          {
            "$ref": "AlphaVectorScalarFunctionTaskExpression",
            "description": "An alpha scalar function task expression within a vector function."
          },
          {
            "$ref": "AlphaVectorVectorFunctionTaskExpression",
            "description": "An alpha vector function task expression."
          },
          {
            "$ref": "AlphaVectorPlaceholderScalarFunctionTaskExpression",
            "description": "A placeholder alpha scalar function task expression within a vector function. Always outputs 0.5."
          },
          {
            "$ref": "AlphaVectorPlaceholderVectorFunctionTaskExpression",
            "description": "A placeholder alpha vector function task expression. Always outputs an equalized vector."
          }
        ],
        "description": "A branch task expression for an alpha vector function."
      },
      "description": "The list of branch tasks to be executed as part of the alpha vector function."
    }
  },
  "required": [
    "type",
    "description",
    "input_schema",
    "tasks"
  ],
  "description": "An alpha vector branch remote function. \"function.json\""
}
