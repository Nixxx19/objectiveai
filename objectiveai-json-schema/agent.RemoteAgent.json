{
  "title": "agent.RemoteAgent",
  "description": "A validated remote Agent with metadata and computed content-addressed ID.",
  "type": "object",
  "$ref": "agent.InlineAgent",
  "properties": {
    "description": {
      "type": "string"
    }
  }
}
