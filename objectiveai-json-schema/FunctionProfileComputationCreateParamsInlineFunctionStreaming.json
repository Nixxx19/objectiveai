{
  "type": "object",
  "properties": {
    "retry_token": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "description": "The retry token provided by a previous incomplete or failed profile computation."
    },
    "from_cache": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "description": "If true, vector completion tasks use cached votes from the global ObjectiveAI votes cache when available. Has lower priority than `retry_token`, higher priority than `from_rng`."
    },
    "from_rng": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "description": "If true, any remaining votes from vector completion tasks are generated via RNG. Has lower priority than `retry_token` or `from_cache`."
    },
    "upstreams": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "$ref": "Upstream",
            "description": "The upstream provider that served the request."
          }
        },
        {
          "type": "null"
        }
      ],
      "description": "Available upstreams for this request."
    },
    "max_retries": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "description": "The maximum number of retries to attempt when a function execution fails during profile computation."
    },
    "n": {
      "type": "integer",
      "description": "The number of function executions to perform per dataset item. Generally speaking, higher N values increase the quality of the computed profile."
    },
    "dataset": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "input": {
            "$ref": "InputValue",
            "description": "A value provided as input to a function."
          },
          "target": {
            "anyOf": [
              {
                "type": "object",
                "properties": {
                  "type": {
                    "const": "scalar"
                  },
                  "value": {
                    "type": "number"
                  }
                },
                "required": [
                  "type",
                  "value"
                ],
                "description": "A scalar target output. The desired output is this exact scalar."
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "const": "vector"
                  },
                  "value": {
                    "type": "array",
                    "items": {
                      "type": "number"
                    }
                  }
                },
                "required": [
                  "type",
                  "value"
                ],
                "description": "A vector target output. The desired output is this exact vector."
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "const": "vector_winner"
                  },
                  "value": {
                    "type": "integer"
                  }
                },
                "required": [
                  "type",
                  "value"
                ],
                "description": "A vector winner target output. The desired output is a vector where the highest value is at the specified index."
              }
            ],
            "description": "The target output for a given function input."
          }
        },
        "required": [
          "input",
          "target"
        ],
        "description": "A Function input and its corresponding target output."
      },
      "description": "The dataset of input and target output pairs to use for computing the profile."
    },
    "ensemble": {
      "anyOf": [
        {
          "type": "string",
          "description": "The unique identifier for the Ensemble."
        },
        {
          "$ref": "EnsembleBase",
          "description": "An ensemble of LLMs."
        }
      ],
      "description": "The Ensemble to use for this completion. May be a unique ID or an inline definition."
    },
    "provider": {
      "anyOf": [
        {
          "$ref": "CompletionProvider",
          "description": "Options for selecting the upstream provider of this completion."
        },
        {
          "type": "null"
        }
      ]
    },
    "seed": {
      "anyOf": [
        {
          "description": "If specified, upstream systems will make a best effort to sample deterministically, such that repeated requests with the same seed and parameters should return the same result."
        },
        {
          "type": "null"
        }
      ]
    },
    "backoff_max_elapsed_time": {
      "anyOf": [
        {
          "type": "integer",
          "description": "The maximum total time in milliseconds to spend on retries when a transient error occurs."
        },
        {
          "type": "null"
        }
      ]
    },
    "first_chunk_timeout": {
      "anyOf": [
        {
          "type": "integer",
          "description": "The maximum time in milliseconds to wait for the first chunk of a streaming response."
        },
        {
          "type": "null"
        }
      ]
    },
    "other_chunk_timeout": {
      "anyOf": [
        {
          "type": "integer",
          "description": "The maximum time in milliseconds to wait between subsequent chunks of a streaming response."
        },
        {
          "type": "null"
        }
      ]
    },
    "function": {
      "anyOf": [
        {
          "$ref": "InlineScalarFunction",
          "description": "A scalar function defined inline. Each task's output expression must return a number in [0,1]. The function's output is the weighted average of all task outputs using profile weights. If there is only one task, its output becomes the function's output directly."
        },
        {
          "$ref": "InlineVectorFunction",
          "description": "A vector function defined inline. Each task's output expression must return an array of numbers summing to ~1. The function's output is the weighted average of all task outputs using profile weights. If there is only one task, its output becomes the function's output directly."
        }
      ],
      "description": "A function defined inline."
    },
    "stream": {
      "const": true,
      "description": "Whether to stream the response as a series of chunks."
    }
  },
  "required": [
    "n",
    "dataset",
    "ensemble",
    "function",
    "stream"
  ],
  "description": "Parameters for computing a function profile for an inline function and streaming the response."
}
