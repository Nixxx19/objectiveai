{
  "title": "swarm.InlineSwarmBaseOrRemoteCommitOptional",
  "description": "Like [`InlineSwarmBaseOrRemote`] but with optional commit.\nUsed in request types where commit resolution happens server-side.",
  "anyOf": [
    {
      "$ref": "swarm.InlineSwarmBase"
    },
    {
      "$ref": "RemotePathCommitOptional"
    }
  ]
}
