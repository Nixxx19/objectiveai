{
  "title": "functions.inventions.VectorLeafTaskObject",
  "description": "Schema for AppendTask on vector leaf functions.\nTakes a `task` property: a JSON string conforming to `functions.alpha_vector.LeafTaskExpression`.",
  "type": "object",
  "properties": {
    "task": {
      "type": "string"
    }
  }
}
