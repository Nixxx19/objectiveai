{
  "title": "functions.inventions.ScalarLeafTaskObject",
  "description": "Schema for AppendTask on scalar leaf functions.\nTakes a `task` property: a JSON string conforming to `functions.alpha_scalar.LeafTaskExpression`.",
  "type": "object",
  "properties": {
    "task": {
      "type": "string"
    }
  }
}
