{
  "title": "filesystem.logs.LogReferenceTag",
  "description": "Constant `\"reference\"` discriminator — the `\"type\"` field on a\n`LogReference`. Exists as its own type so the wire shape can't\ndrift to other strings.",
  "type": "string",
  "enum": [
    "reference"
  ]
}
