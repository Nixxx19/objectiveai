{
  "title": "agent.openrouter.Reasoning",
  "description": "Configuration for model reasoning/thinking capabilities.\n\nSome models (like o1, o3, Claude with extended thinking) support\nexplicit reasoning modes where they can \"think\" before responding.\nThis struct configures those capabilities.\n\n**Note:** The `max_tokens`, `effort`, and `summary_verbosity` fields are\nonly supported by some models. Unsupported fields are silently ignored.",
  "type": "object",
  "properties": {
    "effort": {
      "description": "The reasoning effort level.\n\nOnly supported by some models.",
      "anyOf": [
        {
          "$ref": "agent.openrouter.ReasoningEffort"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "enabled": {
      "description": "Whether reasoning is enabled. Defaults to `true` if other fields are set.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "max_tokens": {
      "description": "Maximum tokens for the reasoning/thinking output.\n\nOnly supported by some models.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "summary_verbosity": {
      "description": "Verbosity of reasoning summaries in the response.\n\nOnly supported by some models.",
      "anyOf": [
        {
          "$ref": "agent.openrouter.ReasoningSummaryVerbosity"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
