{
  "title": "agent.AgentBase",
  "description": "An Agent base definition, either remote (with metadata) or inline.",
  "anyOf": [
    {
      "$ref": "agent.RemoteAgentBase"
    },
    {
      "$ref": "agent.InlineAgentBase"
    }
  ]
}
