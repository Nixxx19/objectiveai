{
  "type": "object",
  "properties": {
    "type": {
      "const": "function"
    },
    "id": {
      "anyOf": [
        {
          "type": "string",
          "description": "The unique identifier for the tool call."
        },
        {
          "$ref": "Expression",
          "description": "An expression which evaluates to a string. Receives: `input`, `map` (if mapped)."
        }
      ],
      "description": "The unique identifier for the tool call."
    },
    "function": {
      "type": "object",
      "properties": {
        "name": {
          "anyOf": [
            {
              "type": "string",
              "description": "The name of the function called."
            },
            {
              "$ref": "Expression",
              "description": "An expression which evaluates to a string. Receives: `input`, `map` (if mapped)."
            }
          ],
          "description": "The name of the function called."
        },
        "arguments": {
          "anyOf": [
            {
              "type": "string",
              "description": "The arguments passed to the function."
            },
            {
              "$ref": "Expression",
              "description": "An expression which evaluates to a string. Receives: `input`, `map` (if mapped)."
            }
          ],
          "description": "The arguments passed to the function."
        }
      },
      "required": [
        "name",
        "arguments"
      ],
      "description": "The name and arguments of the function called."
    }
  },
  "required": [
    "type",
    "id",
    "function"
  ],
  "description": "A function tool call made by the assistant."
}
