{
  "anyOf": [
    {
      "type": "object",
      "properties": {
        "type": {
          "const": "text"
        },
        "text": {
          "anyOf": [
            {
              "type": "string",
              "description": "The text content."
            },
            {
              "$ref": "Expression",
              "description": "An expression which evaluates to the text content. Receives: `input`, `map` (if mapped)."
            }
          ]
        }
      },
      "required": [
        "type",
        "text"
      ]
    },
    {
      "$ref": "Expression",
      "description": "An expression which evaluates to a simple content part. Receives: `input`, `map` (if mapped)."
    }
  ],
  "description": "A simple content part."
}
