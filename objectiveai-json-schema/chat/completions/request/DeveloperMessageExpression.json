{
  "type": "object",
  "properties": {
    "role": {
      "const": "developer"
    },
    "content": {
      "anyOf": [
        {
          "type": "string",
          "description": "Plain text content."
        },
        {
          "type": "array",
          "items": {
            "$ref": "SimpleContentPartExpression",
            "description": "A simple content part."
          },
          "description": "An array of simple content parts."
        },
        {
          "$ref": "Expression",
          "description": "An expression which evaluates to simple content. Receives: `input`, `map` (if mapped)."
        }
      ],
      "description": "Simple content."
    },
    "name": {
      "anyOf": [
        {
          "anyOf": [
            {
              "type": "string",
              "description": "An optional name for the participant. Provides the model information to differentiate between participants of the same role."
            },
            {
              "$ref": "Expression",
              "description": "An expression which evaluates to a string. Receives: `input`, `map` (if mapped)."
            }
          ],
          "description": "An optional name for the participant. Provides the model information to differentiate between participants of the same role."
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "role",
    "content"
  ],
  "description": "Developer-provided instructions that the model should follow, regardless of messages sent by the user."
}
