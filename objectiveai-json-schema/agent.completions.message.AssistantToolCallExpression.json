{
  "title": "agent.completions.message.AssistantToolCallExpression",
  "description": "A function call expression.",
  "type": "object",
  "properties": {
    "function": {
      "description": "The function expression.",
      "$ref": "agent.completions.message.AssistantToolCallFunctionExpression.functions.expression.WithExpression"
    },
    "id": {
      "description": "The tool call ID expression.",
      "$ref": "string.functions.expression.WithExpression"
    },
    "type": {
      "type": "string",
      "enum": [
        "function"
      ]
    }
  }
}
