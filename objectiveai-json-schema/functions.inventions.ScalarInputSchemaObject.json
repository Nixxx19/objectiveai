{
  "title": "functions.inventions.ScalarInputSchemaObject",
  "description": "Schema for WriteInputSchema on scalar functions.\nTakes a `schema` property: a JSON string conforming to `functions.expression.ObjectInputSchema`.",
  "type": "object",
  "properties": {
    "schema": {
      "type": "string"
    }
  }
}
