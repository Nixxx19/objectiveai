{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "agent.completions.request.ProviderMaxPrice",
  "description": "Maximum price constraints per token type.",
  "type": "object",
  "properties": {
    "prompt": {
      "description": "Maximum price per prompt token.",
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    },
    "completion": {
      "description": "Maximum price per completion token.",
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    },
    "image": {
      "description": "Maximum price per image.",
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    },
    "audio": {
      "description": "Maximum price per audio second.",
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    },
    "request": {
      "description": "Maximum price per request.",
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    }
  }
}
