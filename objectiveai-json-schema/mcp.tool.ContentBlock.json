{
  "title": "mcp.tool.ContentBlock",
  "description": "A content block that can be used in prompts and tool results.",
  "anyOf": [
    {
      "description": "Text content.",
      "type": "object",
      "$ref": "mcp.tool.TextContent",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "text"
          ]
        }
      }
    },
    {
      "description": "Image content (base64-encoded).",
      "type": "object",
      "$ref": "mcp.tool.ImageContent",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "image"
          ]
        }
      }
    },
    {
      "description": "Audio content (base64-encoded).",
      "type": "object",
      "$ref": "mcp.tool.AudioContent",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "audio"
          ]
        }
      }
    },
    {
      "description": "A resource link.",
      "type": "object",
      "$ref": "mcp.tool.ResourceLink",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "resource_link"
          ]
        }
      }
    },
    {
      "description": "An embedded resource.",
      "type": "object",
      "$ref": "mcp.tool.EmbeddedResource",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "resource"
          ]
        }
      }
    }
  ]
}
