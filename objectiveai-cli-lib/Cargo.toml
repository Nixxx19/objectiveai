[package]
name = "objectiveai-cli-lib"
version = "2.0.3"
edition = "2024"
description = "Library surface for objectiveai-cli: JSON Lines RPC protocol used between the CLI and plugin processes."

[dependencies]
