{
  "type": "alpha.scalar.branch.function",
  "description": "Error test: bad input for sub-function.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": { "type": "string" }
    },
    "required": ["text"]
  },
  "tasks": [
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "mock-1",
      "input": { "$starlark": "{'wrong_field': input['text']}" }
    }
  ]
}
