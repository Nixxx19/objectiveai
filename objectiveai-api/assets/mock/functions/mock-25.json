{
  "type": "scalar.function",
  "description": "Scalar function with only placeholder tasks.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": { "type": "string" }
    },
    "required": ["text"]
  },
  "tasks": [
    {
      "type": "placeholder.scalar.function",
      "input_schema": {
        "type": "object",
        "properties": {
          "text": { "type": "string" }
        },
        "required": ["text"]
      },
      "input": { "$starlark": "{'text': input['text']}" },
      "output": { "$special": "output" }
    },
    {
      "type": "placeholder.scalar.function",
      "input_schema": {
        "type": "object",
        "properties": {
          "text": { "type": "string" }
        },
        "required": ["text"]
      },
      "input": { "$starlark": "{'text': input['text']}" },
      "output": { "$special": "output" }
    }
  ]
}
