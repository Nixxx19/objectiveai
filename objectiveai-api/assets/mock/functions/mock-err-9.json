{
  "type": "alpha.scalar.branch.function",
  "description": "Error test: branch references nonexistent sub-function.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": { "type": "string" }
    },
    "required": ["text"]
  },
  "tasks": [
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "mock-999",
      "input": { "$starlark": "{'text': input['text']}" }
    }
  ]
}
