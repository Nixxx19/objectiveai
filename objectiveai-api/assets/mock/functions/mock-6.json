{
  "type": "alpha.scalar.leaf.function",
  "description": "Scalar with system message and multi-part user content.",
  "input_schema": {
    "type": "object",
    "properties": {
      "subject": {
        "type": "string"
      },
      "body": {
        "type": "string"
      }
    },
    "required": ["subject", "body"]
  },
  "tasks": [
    {
      "type": "vector.completion",
      "messages": {
        "$starlark": "[{'role': 'system', 'content': [{'type': 'text', 'text': 'You are an email classifier.'}]}, {'role': 'user', 'content': [{'type': 'text', 'text': 'Subject: ' + input['subject']}, {'type': 'text', 'text': 'Body: ' + input['body']}]}]"
      },
      "responses": [
        [{"type": "text", "text": "Important"}],
        [{"type": "text", "text": "Not important"}]
      ]
    }
  ]
}
