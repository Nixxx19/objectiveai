{
  "type": "scalar.function",
  "description": "Error test: scalar output out of range.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": { "type": "string" }
    },
    "required": ["text"]
  },
  "tasks": [
    {
      "type": "vector.completion",
      "messages": { "$starlark": "[{'role': 'user', 'content': [{'type': 'text', 'text': input['text']}]}]" },
      "responses": [
        [{"type": "text", "text": "Yes"}],
        [{"type": "text", "text": "No"}]
      ],
      "output": { "$starlark": "-1.0" }
    }
  ]
}
