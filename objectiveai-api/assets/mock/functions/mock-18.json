{
  "type": "alpha.scalar.branch.function",
  "description": "Scalar super branch with two scalar branch sub-functions.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "subject": {
        "type": "string"
      }
    },
    "required": ["text", "subject"]
  },
  "tasks": [
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "mock-9",
      "input": {
        "$starlark": "{'text': input['text'], 'subject': input['subject']}"
      }
    },
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "mock-10",
      "input": {
        "$starlark": "{'text': input['text']}"
      }
    }
  ]
}
