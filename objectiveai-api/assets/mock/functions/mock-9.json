{
  "type": "alpha.scalar.branch.function",
  "description": "Scalar branch combining spam and importance classifiers.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "subject": {
        "type": "string"
      }
    },
    "required": ["text", "subject"]
  },
  "tasks": [
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "mock-1",
      "input": {
        "$starlark": "{'text': input['text']}"
      }
    },
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "mock-6",
      "input": {
        "$starlark": "{'subject': input['subject'], 'body': input['text']}"
      }
    }
  ]
}
