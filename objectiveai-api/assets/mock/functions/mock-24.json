{
  "type": "scalar.function",
  "description": "Scalar function with mapped branch, non-mapped function task, and 2 VCs.",
  "input_schema": {
    "type": "object",
    "properties": {
      "items": { "type": "array", "items": { "type": "string" } }
    },
    "required": ["items"]
  },
  "tasks": [
    {
      "type": "scalar.function",
      "remote": "mock",
      "name": "mock-9",
      "map": { "$starlark": "len(input['items'])" },
      "input": { "$starlark": "{'text': input['items'][map], 'subject': input['items'][map]}" },
      "output": { "$starlark": "output[0]" }
    },
    {
      "type": "scalar.function",
      "remote": "mock",
      "name": "mock-1",
      "input": { "$starlark": "{'text': input['items'][0]}" },
      "output": { "$special": "output" }
    },
    {
      "type": "vector.completion",
      "messages": { "$starlark": "[{'role': 'user', 'content': [{'type': 'text', 'text': 'Evaluate quality: ' + input['items'][0]}]}]" },
      "responses": [[{"type": "text", "text": "Good"}], [{"type": "text", "text": "Bad"}]],
      "output": { "$special": "task_output_weighted_sum" }
    },
    {
      "type": "vector.completion",
      "messages": { "$starlark": "[{'role': 'user', 'content': [{'type': 'text', 'text': 'Evaluate clarity: ' + input['items'][0]}]}]" },
      "responses": [[{"type": "text", "text": "Clear"}], [{"type": "text", "text": "Unclear"}]],
      "output": { "$special": "task_output_weighted_sum" }
    }
  ]
}
