{
  "type": "alpha.vector.branch.function",
  "description": "Vector branch with mixed tasks and skip conditions.",
  "input_schema": {
    "context": {
      "properties": {
        "query": {
          "type": "string"
        },
        "deep": {
          "type": "boolean"
        }
      },
      "required": ["query"]
    },
    "items": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 2
    }
  },
  "tasks": [
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "owner": "mock",
      "repository": "mock-1",
      "commit": "mock",
      "input": {
        "$starlark": "{'text': input['context']['query']}"
      }
    },
    {
      "type": "alpha.vector.function",
      "remote": "mock",
      "owner": "mock",
      "repository": "mock-4",
      "commit": "mock",
      "input": {
        "items": {
          "$starlark": "input['items']"
        }
      }
    },
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "owner": "mock",
      "repository": "mock-6",
      "commit": "mock",
      "skip": {
        "$starlark": "not input['context'].get('deep', False)"
      },
      "input": {
        "$starlark": "{'subject': input['context']['query'], 'body': input['context']['query']}"
      }
    },
    {
      "type": "alpha.vector.function",
      "remote": "mock",
      "owner": "mock",
      "repository": "mock-5",
      "commit": "mock",
      "skip": {
        "$starlark": "not input['context'].get('deep', False)"
      },
      "input": {
        "context": {
          "$starlark": "input['context']"
        },
        "items": {
          "$starlark": "input['items']"
        }
      }
    }
  ]
}
