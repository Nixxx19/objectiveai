{
  "description": "Two agents for scalar branch with skip.",
  "swarm": {
    "agents": [
      {
        "upstream": "mock",
        "output_mode": "tool_call"
      },
      {
        "upstream": "mock",
        "output_mode": "json_schema"
      }
    ]
  },
  "profile": [0.55, 0.45]
}
