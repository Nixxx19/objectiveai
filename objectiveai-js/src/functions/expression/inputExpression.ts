import { z } from "zod";
import { AgentCompletionsMessageRichContentPartSchema } from "../../agent/completions/message/richContentPart";
import { FunctionsExpressionWithExpressionFunctionsExpressionInputExpressionSchema } from "./withExpression";

export const FunctionsExpressionInputExpressionSchema = z.union([AgentCompletionsMessageRichContentPartSchema.describe("Rich content (image, audio, video, file)."), z.record(z.string(), z.lazy(() => FunctionsExpressionWithExpressionFunctionsExpressionInputExpressionSchema)).describe("An object with values that may be expressions."), z.array(z.lazy(() => FunctionsExpressionWithExpressionFunctionsExpressionInputExpressionSchema)).describe("An array with elements that may be expressions."), z.string().describe("A string value."), z.number().int().meta({ format: "int64" }).describe("An integer value."), z.number().meta({ format: "double" }).describe("A floating-point number."), z.boolean().describe("A boolean value.")]).describe("An input value that may contain expressions (pre-compilation).\n\nSimilar to [`Input`] but object values and array elements can be\nexpressions (JMESPath or Starlark) that are evaluated during compilation.").meta({ title: "functions.expression.InputExpression" });
export type FunctionsExpressionInputExpression = z.infer<typeof FunctionsExpressionInputExpressionSchema>;
