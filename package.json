{
  "name": "objectiveai-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter objectiveai-web run dev",
    "build:js": "pnpm --filter objectiveai run build",
    "build:tree": "pnpm --filter @objectiveai/function-tree run build",
    "build:web": "pnpm --filter objectiveai-web run build",
    "build": "pnpm run build:js && pnpm run build:tree && pnpm run build:web"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  }
}
